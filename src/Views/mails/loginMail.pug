<!DOCTYPE html>
html
    head
        meta(charset='utf-8')
        style.
            .text-center { text-align: center; }
    body
        p.text-center
            a(href="#{url}" target="_blank") Click here to connect
